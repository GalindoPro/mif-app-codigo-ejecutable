DATABASE_URL="postgresql://usuario:password@localhost:5432/mif_dev?schema=public"
JWT_SECRET="cambia-este-secreto-en-produccion-por-uno-largo-y-aleatorio"
JWT_EXPIRES_IN="8h"
PORT=4000
CORS_ORIGIN="http://localhost:5173"
# Opcional: contraseña temporal fija para los usuarios creados por `npm run db:seed`.
# Si se omite, se genera una aleatoria y se imprime una sola vez en consola.
SEED_PASSWORD=
